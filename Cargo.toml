[package]
name = "fin_converter_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
fin_converter_lib = { git = "https://github.com/jekispank98/fin_converter_lib.git" }
clap = { version = "4", features = ["derive"] }
chrono = "0.4"
strum = { version = "0.27", features = ["derive"] }
